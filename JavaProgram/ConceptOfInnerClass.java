import java.util.*;
import java.time.LocalDate;
import java.time.format.DateTimeFormatter;

interface features{
    void model(String string);
    void mfgyear(LocalDate date);
    void color(String string);
    void fueltype(String string);
    void mileage(int mileage);
}

class carPurchase implements features{
    HashMap<String,String> garage = new HashMap<>();

    public void model(String model_number){
        if(garage.containsKey(model_number)){
            private class CarDetails{

            }
        }
    }
}
public class ConceptOfInnerClass{
    
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        sc.close();
    }
}